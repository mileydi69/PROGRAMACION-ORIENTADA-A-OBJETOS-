class vehiculos:

def __init__(self, marcas, modelos, color,kilometrajes):
        self.marcas = marcas
        self.modelos = modelos
        self.color = color
        self.kilometrajes = kilometrajes

def atributos(self):
        print(self.marcas,":", sep="")
        print(".modelos:", self.modelos)
        print(".color", self.color)
        print(".kilometraje", self.kilometrajes)

def subir_nivel(self, modelos, color, kilometrajes):
        self.marcas = self.marcas + 'marcas'
        self.modelos = self.modelos + modelos
        self.color = self.color + color

 def es_rojo(self):
        return self.color > 0

def de_paquete(self):
        self.nuevo = 0
        print(self.marcas, "toyota")

def daño(self, choque):
        return self.color - 'modelos.kilometraje'

def arreglar(self, tecnico):
        daño = self.daño('choque')
        choque_nuevo = 'choque_nuevo_daño'
        print(self.marcas, "ha realizado", "arreglo", "puntos de daño a", "choque_toyota")
        if choque_nuevo in tecnico:
            print("Vida de", "choque_arreglo", "es", choque_nuevo)
        else:
            "choque_morir()"